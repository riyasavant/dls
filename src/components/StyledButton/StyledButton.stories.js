import StyledButton from "./StyledButton";

export default {
    title: 'StyledButton'
};

export const StyledButtonComp = () => {
    return (
        <StyledButton text={'Click here'} url={'#'}></StyledButton>
    )
}