import Tabs from "./Tabs";

export default {
    title: 'Tabs'
};

export const BasicTable = () => {
    return (
        <div style={{width: '25%'}}>
            <Tabs
            />
        </div>
    )
}